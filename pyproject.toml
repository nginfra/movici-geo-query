[build-system]
build-backend = "setuptools.build_meta"
requires = ["setuptools>=42", "wheel", "pybind11~=2.10.1"]

[tool.black]
exclude = '''(
/(
  | \.direnv
  | \.eggs
  | \.git
  | \.venv
  | _build
  | build
  | dist
)/
)
'''
include = '\.pyi?$'
line-length = 99
